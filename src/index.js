import React from 'react';
import ReactDOM from 'react-dom/client';

function Tailwind () {
    return (
        <div>
            tailwind
        </div>
    )
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<Tailwind />)